---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: applicationintents.an.openshift.io
spec:
  group: an.openshift.io
  names:
    kind: ApplicationIntent
    listKind: ApplicationIntentList
    plural: applicationintents
    singular: applicationintent
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.target.deployment
      name: Target
      type: string
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .status.currentP99Ms
      name: P99ms
      type: integer
    - jsonPath: .status.currentReplicas
      name: Replicas
      type: integer
    - jsonPath: .spec.sla.p99LatencyMs
      name: SLA
      type: integer
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ApplicationIntent is the Schema for the applicationintents API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ApplicationIntent
            properties:
              constraints:
                description: constraints defines scaling boundaries
                properties:
                  maxReplicas:
                    format: int32
                    minimum: 1
                    type: integer
                  minReplicas:
                    default: 1
                    format: int32
                    minimum: 1
                    type: integer
                required:
                - maxReplicas
                - minReplicas
                type: object
              sla:
                description: sla defines the service level agreement
                properties:
                  p99LatencyMs:
                    description: p99Latency target in milliseconds
                    format: int64
                    minimum: 1
                    type: integer
                required:
                - p99LatencyMs
                type: object
              target:
                description: target identifies the deployment to scale
                properties:
                  deployment:
                    description: deployment name
                    minLength: 1
                    type: string
                  namespace:
                    description: namespace of the deployment (defaults to the intent's
                      namespace)
                    type: string
                required:
                - deployment
                type: object
            required:
            - constraints
            - sla
            - target
            type: object
          status:
            description: status defines the observed state of ApplicationIntent
            properties:
              currentP99Ms:
                description: currentP99Ms is the observed p99 latency in milliseconds
                format: int64
                type: integer
              currentReplicas:
                description: currentReplicas is the current replica count
                format: int32
                type: integer
              lastUpdated:
                description: lastUpdated timestamp
                format: date-time
                type: string
              message:
                description: message describes the current state
                type: string
              state:
                description: 'state of the intent: Fulfilled, Degraded, Scaling, Unknown'
                enum:
                - Fulfilled
                - Degraded
                - Scaling
                - Unknown
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
